# API リファレンス

## TerminationCondition クラス

最適化の終了条件を記述する。

| パラメータ | デフォルト | 説明 |
|---|---|---|
| `maxIter` | `INT_MAX` | 最大世代数 |
| `timeLimit` | `DBL_MAX` | 実行時間の上限（秒） |
| `xtol` | `0` | パラメータ空間における最適解の近傍の大きさの基準値（DE では無効） |
| `ftolAbs` | `0` | 目的関数の改善率の基準値 |
| `ftolRel` | `0` | 目的関数の改善率の基準値 |
| `targetError` | `0` | 目的関数が目標値を下回ったときに終了 |
| `stall` | `INT_MAX` | 停滞期間の基準値（世代数）。`\|bestSoFar - currentBest\| < ftolAbs + ftolRel * bestSoFar` の状態が stall 世代続いたら終了 |

---

## NasapFit クラス

### `from_yaml`

```python
def from_yaml(
    config_yaml_path: str | Path,
    *,
    project_root: str | Path | None = None,
    validate_files: bool = True,
) -> "NasapFit"
```

NASAP_fit を設定を記述した `.yaml` ファイルから構築する。

**引数**
- `config_yaml_path` — CWD からの `.yaml` ファイルへの相対パス

---

### `calc_error`

```python
def calc_error(constants: Sequence[float]) -> float
```

与えられた反応速度定数に対してシミュレーションを行い、QASAP との残差平方和を求める。

**引数**
- `constants` — 反応速度定数の数列

**返り値**
- QASAP との残差平方和

---

### `calc_nrmse`

```python
def calc_nrmse(self, error: float) -> float
```

残差平方和を正規化した値を返す。

---

### `reaction_count`

```python
@property
def reaction_count(self) -> int
```

与えられた Reaction Network ファイル内の反応の数を返す。

---

### `rate_constant_index`

```python
@property
def rate_constant_index(self) -> Mapping[str, int]
```

Reaction Network ファイル内での反応速度定数ラベルと、反応速度定数の数列における index との対応を返す。

---

### `run_de`

```python
def run_de(
    pop_size: int,
    termination_condition: Mapping[str, Any],
    lower_bound: float = 1e-3,
    upper_bound: float = 1e4,
    seed: int = 1,
) -> list[Any]
```

`[lower_bound, upper_bound)` の間で指数分布に基づいてランダムに速度定数の初期値を生成し、差分進化法を実行する。

**引数**
- `pop_size` — 速度定数の組の数（`>= 3`）
- `termination_condition` — 最適化の終了条件
- `lower_bound` — ランダム生成される速度定数の下限
- `upper_bound` — ランダム生成される速度定数の上限
- `seed` — シード値

**返り値**
- 最適化終了後の速度定数の組の配列（残差平方和の小さいものから昇順）

---

### `run_de_from_population`

```python
def run_de_from_population(
    population: Sequence[Sequence[float]],
    termination_condition: Mapping[str, Any],
    seed: int = 1,
) -> list[Any]
```

**引数**
- `population` — 初期値として与える速度定数の組の配列
- `termination_condition` — 最適化の終了条件
- `seed` — シード値

**返り値**
- 最適化終了後の速度定数の組の配列

---

### `run_lm`

```python
def run_lm(initial_constants: Sequence[float], termination_condition: Mapping[str, Any]) -> Any
```

Levenberg–Marquardt 法を用いて最適化を行う。

**引数**
- `initial_constants` — Levenberg–Marquardt 法の初期値
- `termination_condition` — 最適化の終了条件

**返り値**
- 最適化終了後の速度定数の組

---

### `run_lm_batch`

```python
def run_lm_batch(
    initial_constants: Sequence[Sequence[float]],
    termination_condition: Mapping[str, Any],
) -> list[Any]
```

与えられた速度定数の組の配列に対して並列に Levenberg–Marquardt 法を行う。MPI が利用可能な場合のみ有効。

**引数**
- `initial_constants` — Levenberg–Marquardt 法の初期値の配列
- `termination_condition` — 最適化の終了条件

**返り値**
- 最適化終了後の速度定数の組の配列

---

### `gauss_newton_hessian`

```python
def gauss_newton_hessian(self, point: Sequence[float]) -> list[list[float]]
```

q=logp として、f(e^q) の q に関するヘッシアンのガウス・ニュートン近似を返す。
すなわち、∂f(p)/∂logpを計算する。

**引数**
- `point` — 速度定数の組

**返り値**
- ヘッセ行列の近似

---

### `calc_hessian`

```python
def calc_hessian(self, point: Sequence[float]) -> list[list[float]]
```


q=logp として、f(e^q) の q に関するヘッシアンを返す。
すなわち、∂^{2}f(p)/∂logp ∂logp^trを計算する。

**引数**
- `point` — 速度定数の組

**返り値**
- ヘッセ行列

---

### `simulate`

```python
def simulate(
    t: Sequence[float],
    constants: Sequence[float],
    reaction_ids: Iterable[int],
) -> Any
```

与えられた各 `t` における化学種の濃度、および反応の進行度を求める。

**引数**
- `t` — 追跡する時点
- `constants` — 速度定数の組
- `reaction_ids` — 追跡する反応の反応 ID

**返り値** `ReactionProgressResult`
- `timePoints` — `t` の要素数
- `t` — 追跡を行った時点
- `y` — `y[species][time_index]` = `t[time_index]` 分後における `species` 番目の化学種の濃度（二次元配列）
- `reactionProgress`
  - `reaction_ids` — 追跡した反応 ID
  - `J` — `J[reaction_id][time_index]` = `t[time_index]` 分後における `reaction_id` 番目の反応進行度（二次元配列）
  - `reaction_labels` — `reaction_ids` 番目の反応のラベル（例: `"1 + 2 -> 3 + 4"`）
